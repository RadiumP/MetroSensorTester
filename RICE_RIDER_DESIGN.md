# 《最饿骑士 Rice Rider》游戏设计方案

**版本**：v0.1 设计提案　**世界观**：Rice City 最饿都市（GTA1/2 风格、80-90 年代南方沿海中国、食物主题犯罪）
**载体**：Godot 原生 Android app，通过本地 socket 消费 MetroSensorTester 的实时推断数据
**目标**：轻量型、单局对应一次真实通勤、把"躲避竞速"和"飞行射击"用真实数据的强弱变化自然地缝在一起

---

## 一、核心定位：为什么是"外卖骑手"

Rice City 设定里本来就有"外卖/差评"这条线（top-down 动作冒险 + 送单/差评评分机制），这次不是另起一个新概念，而是把它单独拆出来做成一个和地铁通勤强绑定的轻量分支：玩家在 Rice City 世界观里扮演一名骑手，你在现实中坐地铁的这段时间，就是角色在城里疯狂接单跑腿的这段时间——现实的车速、震动、报站，直接映射成游戏里"路况"的紧张程度。这个题材天然能同时装下两种玩法：

- **躲避竞速**：骑着摩托/三轮车在街上抢时间送单，对应 Rice City 原有的 GTA1/2 俯视角美术风格，改动成本最低。
- **飞行射击**：偶尔遇到"空投加急单"，操控无人机送货，穿插在竞速主线里当高光时刻，而不是从头到尾撑起整个游戏。

两个玩法不是二选一，而是**主次结构**：地面竞速是骨架，飞行射击是被真实数据"点燃"出来的爆发段。下面详细说明。

---

## 二、实时数据 → 玩法映射表

MetroSensorTester 广播的字段（`state` / `train_moving` / `player_active` / `mic_level_ratio` / `accel_rms` / `gyro_rms_deg_s` / `t`）逐一对应到游戏机制，做到"每个字段都在游戏里能被玩家感知到"，而不是只用 `train_moving` 当一个开关：

| 数据字段 | 含义 | 游戏映射 |
|---|---|---|
| `state = 校准中` | 阈值还没建立、刚开始采集 | 开局倒计时/选角画面："今日接单准备中…"，玩家在此时做 Roguelite 首次三选一（选骑手/载具/初始被动） |
| `state = 运行`，`train_moving = true` | 地铁在开 | **主循环：竞速关**打开。自动向前滚动的俯视角赶路段，玩家左右滑屏躲避行人/路障/竞对骑手/城管 |
| `state = 停站` | 地铁进站停靠 | **补给关**：动作暂停，进入"驿站/后厨/黑市摊位"三选一强化卡界面（Roguelite 加成），选完等下一次开动继续 |
| `accel_rms` / `gyro_rms_deg_s`（震动强度） | 车厢颠簸/加速度 | 竞速关的**速度倍率与路障密度**：越颠簸=路况越差=场面越快越乱，直接给玩家"这站开得野"的直观反馈，颠簸剧烈时触发**"极速空投"飞行射击小段**（见下） |
| `mic_level_ratio`（环境噪音比例） | 车厢/站台嘈杂程度 | **围观值/举报值**：噪音越高，"差评抓拍""交警临检"一类负面随机事件刷新率越高，是竞速关的动态 debuff 源，而不是纯装饰 |
| `player_active`（检测到玩家在动） | 玩家可能正在起身/走动（快到站） | **安全阀**：自动降低当前段的精度要求/给出"即将到站，注意扶好手机"的软提示，避免玩家在真实站起来的瞬间还在专心玩快节奏躲避关；同时是"冲刺送达"彩蛋的触发点之一 |
| `t`（累计时长） | 本次采集已进行多久 | 驱动 Roguelite 强度曲线（每隔一段时间基础难度上调）、连击/信誉分计时窗口 |

这样设计的好处：真实数据不是简单地"决定一个难度数字"，而是六个字段分别落在不同的、玩家能明确感知到的系统上（速度/密度/负面事件/安全提示/节奏曲线），车速快和车厢吵是两种完全不同的"烦人法"，比单一难度滑杆有意思得多。

---

## 三、主循环：竞速送单关（运行态）

- **视角与操作**：延续 Rice City 的俯视角像素风格。操作只用触屸滑动/点击变道、点击使用道具——**不用任何手机姿态/倾斜控制**，因为手机的运动数据已经被"吃掉"用来表示真实车况了，而且玩家是在真实地铁上，不适合也不安全用重力感应操作（这点和你们之前放弃重力感应控制玩家移动的决定是一致的）。
- **目标**：在这一段"运行"区间内尽量多送/送好当前手上的订单包裹，避免撞车/超时导致订单变成差评。
- **动态强度**：路障/行人/竞对骑手的生成密度和滚动速度，实时跟随 `accel_rms`/`gyro_rms_deg_s` 平滑插值调整（复用你们在 InferenceEngine 里已经验证过的"平滑+锚定阈值"思路，避免数值抖动导致游戏难度忽高忽低）；`mic_level_ratio` 单独控制"举报/临检"类负面随机事件的刷新率。
- **失败即宽容**：撞车/超时 = 当前订单直接判"差评"、扣一点信誉分或掉一级装备，**不清空整局进度**，跟你们之前定的"宽容式失败"一致——毕竟一局的边界就是"到下车为止"，没必要因为一次小失误就把整个通勤时间的游戏体验清零。

---

## 四、补给关：进站三选一（停站态）

延续原来"车速游戏"里已经定下的结构：进站停车时，动作段自动暂停，弹出一个 Rice City 风味的三选一强化卡界面。给几个方向参考（后续可以再扩充卡池）：

- **装备类**："加装后座保温箱"（可携带订单上限+1）、"改装排气"（基础速度永久+X%）
- **技能类**："别车绝招"（短暂无敌冲刺，可撞飞路障）、"喊话闪避"（清空当前屏幕上一半的负面事件）
- **被动类**："熟客免检"（一次举报事件自动免疫）、"老江湖"（噪音举报刷新率降低）

三选一之外可以加一条"跳过换成信誉分"的选项，避免玩家在不想要任何卡时被迫拿弱卡。

---

## 五、爆发段：极速空投（飞行射击彩蛋）

**触发条件**：`accel_rms`/`gyro_rms_deg_s` 在短时间内出现持续的高峰值（比如列车提速、过一段特别颠的轨道，或者你们后续想模拟的"快车通过"场景），系统判定为"这一段路况极端"，直接切一小段**竖版飞行射击**：玩家的骑手掏出无人机送一单"加急空投"，画面切换成城市天际线俯冲视角，躲避/清除鸽子、电线、竞对无人机，成功送达后拿一个大的连击加成，然后无缝切回地面竞速。

这样设计的意义：

1. 飞行射击不需要单独撑起一整套关卡/经济/成长系统，只是竞速主循环里被数据"点燃"出来的高光时刻，开发量可控，符合"轻量型"目标。
2. 真实数据的**峰值**和**均值**分别喂给两套不同的表现形式（均值→竞速关强度，峰值→触发飞行射击），比"数值越大越难"这种单一映射更能体现"这数据是真实世界发生的事"这个卖点。
3. 视觉和玩法上有落差感，长通勤路上不会一路都是同一种俯视角小人躲障碍物，中间会被地铁本身的真实颠簸"炸"出一段完全不同的体验。

---

## 六、下车结算与跨局养成

一局的边界仍然是"到下车为止"（即 MetroSensorTester 停止采集/socket 断开），不分层。结算画面用 Rice City 已有的"好评/差评"评分体系呈现本次通勤的表现（送单数、连击、差评次数、极速空投触发次数等），转换成"信誉分/饭票"之类的跨局养成货币——因为真实通勤是每天发生的、天然分段的，跨局养成能让"今天上班这一局"和"明天下班那一局"之间产生连续感，而不是每次都从零开始。

---

## 七、和 GameLinkServer 现有协议的对接备注

现有字段已经完全够用，不需要改协议：

- `state` 直接驱动关卡状态机（校准中→竞速→补给→…→下车）
- `train_moving` 作为竞速关的开/关双重校验（跟 `state` 一起用，容错更强）
- `accel_rms` / `gyro_rms_deg_s` 建议在 Godot 端也做一次平滑（比如简单的滑动平均或者和 InferenceEngine 类似的锚定阈值），避免每 250ms 一个 tick 的原始抖动直接体现在游戏画面速度上，观感会很跳
- `mic_level_ratio` 建议先在小范围内实测几组真实数值（安静车厢 vs 报站广播 vs 拥挤早高峰），再定"举报事件"刷新率的具体曲线，避免拍脑袋数值不成比例

---

## 下一步

这套方案目前只是文字设计，接下来的落地顺序建议是：先把状态机（校准中/竞速/补给/结算）在 Godot 里用最简单的方块/占位图跑通，验证"数据驱动关卡切换"这条链路本身是顺的，再逐步替换成正式的俯视角美术和飞行射击彩蛋——跟你们当初先做 `godot-link-test` 验证 socket 链路、再往上叠功能的思路是一致的。

如果这个方向你觉得可以，我们可以接着往下细化：比如具体的关卡状态机代码结构、Rice City 美术基调的具体参考、或者飞行射击彩蛋的触发阈值怎么调。
